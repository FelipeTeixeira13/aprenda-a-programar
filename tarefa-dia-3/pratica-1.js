// Exiba todos os números de 1 a n. Por exemplo, se o valor de n for 9, você deverá exibir o seguinte resultado na tela com console.log()

let contador = 1

while (contador <= 9 ) {
    console.log (contador)
    contador = contador + 1
}